<%= form_for(@sample) do |f| %>
  <% if @sample.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sample.errors.count, "error") %> prohibited this sample from being saved:</h2>

      <ul>
      <% @sample.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :dated_at %><br>
    <%= f.date_select :dated_at, use_month_numbers: true, use_separators: true %>
  </div>
  <div class="field">
    <%= f.label :datetimed_at %><br>
    <%= f.datetime_select :datetimed_at, use_separators: { inline: true } %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
